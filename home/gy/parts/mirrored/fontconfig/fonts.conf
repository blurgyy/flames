<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<fontconfig>

  <!-- Load fontconfig files from packages installed via home manager -->
  <include ignore_missing="yes">~/.nix-profile/etc/fonts/conf.d</include>

<!--
  vim: ft=xml ts=2 sts=2 sw=2 et:
-->
</fontconfig>
